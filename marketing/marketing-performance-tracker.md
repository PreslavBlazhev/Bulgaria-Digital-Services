# Маркетинг performance tracker

**Дата:** 27.08.2026 (Phase 18), преработен 28.08.2026 · Схема и формули.
**Няма реални данни.**

---

## Одит: какво вече съществува

> **Поправка от 28.08.2026.** Първата версия на този документ твърдеше, че
> `.xlsx` няма. Търсенето тогава е било само в папката на проекта. Извън нея
> има работеща таблица и тя има предимство пред този документ.

### Съществуващата таблица

## `C:\Users\The King\Desktop\BDS_Marketing_Performance_Tracker.xlsx`

Създадена 26.08.2026. Три листа, вече със свързани формули:

| Лист | Съдържание |
|---|---|
| `Ad Performance` | таблица `AdPerformanceTable`, диапазон `A1:O501` — 500 реда за данни |
| `Dashboard` | обобщение върху целия диапазон |
| `Pivot Analysis` | разбивка по канал със `SUMIF` |

**Колони в `Ad Performance` (A→O):**

`Date` · `Channel` · `Campaign` · `Spend` · `Impressions` · `Clicks` · `CTR` ·
`CPC` · `Leads` · `CPL` · `Clients` · `Revenue` · `CAC` · `ROAS` · `Profit`

**Състояние на данните:** празна. Нула реални разходи, заявки, клиенти и
приходи. Формулите са налице, стойностите — не. Това е правилното изходно
състояние.

### Как се отнася към изискванията на Phase 18

| Изискване | В таблицата |
|---|---|
| Дневна гранулярност `Date + Channel + Campaign` | ✅ |
| `CTR = Clicks / Impressions` | ✅ `IF(OR(F2="",E2="",E2=0),"",F2/E2)` |
| `CPC = Spend / Clicks` | ✅ |
| `CPL = Spend / Leads` | ✅ |
| `CAC = Spend / Clients` | ✅ |
| `ROAS = Revenue / Spend` | ✅ |
| Без `Infinity` / `NaN` / деление на нула | ✅ пази се и в трите листа — `IF(OR(...))` в таблицата, `IFERROR` в Dashboard и Pivot |
| Колона `Notes` | ❌ няма |
| Наименование на `Revenue − Spend` | ⚠️ казва се **`Profit`** |

### Единственото реално несъответствие

Колона `O` се казва `Profit` и смята `Revenue − Spend`. Това е точно името,
което Phase 18.4 казва да не се използва: в него няма нито един оперативен
разход — нито време, нито инструменти, нито данъци. При два продадени проекта
и малък рекламен бюджет ще показва число, което изглежда като печалба, а не е.

**Препоръка:** колоната да се преименува на `Marketing Contribution`, а
етикетът в `Dashboard` — също. Формулата остава същата.

**Не е направено.** Файлът е на собственика, извън проекта, и преименуването
сменя и надписа в Dashboard. Ако решението е да остане `Profit`, тогава трябва
да се дефинират и въведат реалните оперативни разходи — иначе името лъже.

### Ролята на този документ

Документът **не създава втори tracker.** Той описва схемата, формулите и
правилата, по които таблицата се води, и служи за:

* дефиниция какво значи всяка колона и откъде идва;
* правилата за деление на нула и за неплатените канали;
* `marketing-performance-template.csv` — за случая, в който данните тръгнат
  към Google Sheets или към автоматичен импорт (Phase 19), вместо към Excel.

| Останалото в проекта | |
|---|---|
| `google-ads-restaurants-import.csv` | импорт за Google Ads Editor, не tracker |
| Друга таблица с реални данни | няма |

---

## Гранулярност

## Един ред = `Дата` + `Канал` + `Кампания`

Не по седмица, не по месец. Дневната гранулярност е единственото, което
позволява да се види ефектът от промяна — паузирана ключова дума, нов рекламен
текст, вдигнат бюджет.

Седмичните и месечните числа се получават чрез сумиране. Обратното е
невъзможно.

---

## Колони

### Въвеждат се (от рекламните платформи)

| Колона | Тип | Източник |
|---|---|---|
| `Date` | дата | ден на отчета |
| `Channel` | изброимо | Google Ads · Meta Ads · Organic · Referral · Direct |
| `Campaign` | текст | име на кампанията |
| `Spend` | € | платформата |
| `Impressions` | цяло | платформата |
| `Clicks` | цяло | платформата |

### Въвеждат се (от CRM)

| Колона | Тип | Източник |
|---|---|---|
| `Leads` | цяло | брой заявки с този `Source Category` за деня |
| `Clients` | цяло | брой `Won` сделки, отнесени към канала |
| `Revenue` | € | сума на `Deal Value` за спечелените |

### Пресмятат се — **никога не се въвеждат на ръка**

| Колона | Формула |
|---|---|
| `CTR` | `Clicks / Impressions` |
| `CPC` | `Spend / Clicks` |
| `CPL` | `Spend / Leads` |
| `CAC` | `Spend / Clients` |
| `ROAS` | `Revenue / Spend` |
| `Marketing Contribution` | `Revenue − Spend` |

---

## Защо „Marketing Contribution“, а не „Profit“

`Revenue − Ad Spend` **не е печалба.** Печалбата изисква да се извадят и:

- времето за изработка (най-големият реален разход при услуга)
- хостинг, домейни, инструменти
- такси на платежни доставчици
- данъци
- времето за продажби и поддръжка

Ако тази колона се нарече „Profit“, числото ще изглежда добре и ще води до
грешни решения — например вдигане на бюджет при проект, който всъщност губи
пари заради вложените часове.

## `Marketing Contribution = Revenue − Ad Spend`

Тя отговаря на един въпрос: **носи ли рекламата повече, отколкото струва.**
Това е достатъчно, за да се реши дали да продължи.

> **Кога може да стане истинска печалба:** когато има дефинирана часова
> ставка и се записват вложените часове по проект. Тогава:
> `Profit = Revenue − Ad Spend − (часове × ставка) − оперативни разходи`
> Дотогава името „Profit“ не се използва.

---

## Деление на нула

При нов акаунт **всяка** от тези колони почва с нула. Ако формулите не се
пазят, таблицата се пълни с `#DIV/0!`, `Infinity` и `NaN` — и става
неизползваема още първата седмица.

**Правилото: празно, а не нула и не грешка.**

Нула CTR е твърдение („никой не кликна“). Липсваща стойност е признание
(„няма показвания, няма какво да се смята“). Разликата има значение при
осредняване.

| Колона | Условие | Резултат |
|---|---|---|
| `CTR` | `Impressions = 0` | празно |
| `CPC` | `Clicks = 0` | празно |
| `CPL` | `Leads = 0` | празно |
| `CAC` | `Clients = 0` | празно |
| `ROAS` | `Spend = 0` | празно |
| `Marketing Contribution` | винаги | смята се, може да е отрицателна |

### Google Sheets / Excel

```
CTR   =IF(Impressions=0, "", Clicks/Impressions)
CPC   =IF(Clicks=0,      "", Spend/Clicks)
CPL   =IF(Leads=0,       "", Spend/Leads)
CAC   =IF(Clients=0,     "", Spend/Clients)
ROAS  =IF(Spend=0,       "", Revenue/Spend)
MC    =Revenue-Spend
```

`Marketing Contribution` **трябва** да може да е отрицателна. Първите седмици
почти сигурно ще е — има разход преди първия клиент. Скриването ѝ би скрило
точно това, което трябва да се вижда.

---

## Канали

| Канал | Плащаме ли | `Spend` |
|---|---|---|
| **Google Ads** | да | реален |
| **Meta Ads** | да | реален |
| **Organic** | не | **винаги 0** |
| **Referral** | не | **винаги 0** |
| **Direct** | не | **винаги 0** |

### Защо неплатените канали изобщо са в таблицата

Защото носят заявки. Ако само платените са в таблицата, сравнението е
безсмислено — няма спрямо какво.

### Какво се случва с CPL и CAC при `Spend = 0`

Стават **празни**, не нула. Заявка от organic не струва 0 € — струва време,
което таблицата не мери. Показването на „CPL = 0“ би било невярно твърдение,
не липса на данни.

**ROAS също остава празен** при `Spend = 0`. Деление на нула няма смисъл,
независимо колко е приходът.

`Marketing Contribution` при неплатен канал = целият приход. Това е коректно:
приход без рекламен разход.

### Съответствие със `Source Category`

Колоната `Channel` ползва човешките имена; CRM-ът ползва машинните от
класификатора в Phase 11:

| `Channel` | `Source Category` |
|---|---|
| Google Ads | `google_ads` |
| Meta Ads | `meta_ads` |
| Organic | `organic_google` |
| Referral | `referral`, `instagram`, `facebook` |
| Direct | `direct` |
| — | `other` → преглежда се ръчно |

`other` няма свой канал нарочно. Ако започне да расте, значи има източник,
който класификаторът не разпознава — и това е сигнал да се погледне, не да се
скрие в кофа.

---

## Кое се гледа и кое не

### Основни бизнес показатели

## `Leads` · `Qualified Leads` · `Clients` · `CAC` · `Revenue` · `ROAS`

### Второстепенни — диагностика, не цел

`CTR` · `CPC` · `Impressions` · позиция

**Евтините кликове не са успешна кампания.** CTR и CPC обясняват *защо*
основните показатели са такива — не ги заместват. Кампания с CTR 12% и нула
квалифицирани заявки е провал, независимо колко добре изглежда.

> `Qualified Leads` не е отделна колона в дневната таблица, защото
> квалификацията се случва след разговор, дни по-късно. Идва от CRM-а при
> месечния преглед — броят заявки, стигнали поне до `Qualified`.

---

## Пример за структура

Реални колони, **измислени числа за илюстрация на формулите** — не са данни:

| Date | Channel | Campaign | Spend | Impr. | Clicks | CTR | CPC | Leads | CPL | Clients | CAC | Revenue | ROAS | MC |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| — | Google Ads | restaurant_search_bg | — | — | — | — | — | — | — | — | — | — | — | — |
| — | Organic | — | 0 | — | — | — | *(празно)* | — | *(празно)* | — | *(празно)* | — | *(празно)* | — |

Празният ред е нарочен. Таблицата се пуска празна и се пълни с реални числа —
примерни стойности в tracker имат навика да оцелеят до първия отчет.

---

## Ритъм на попълване

| Кога | Какво |
|---|---|
| **Ежедневно** | `Spend`, `Impressions`, `Clicks` от платформите; `Leads` от CRM |
| **При затваряне на сделка** | `Clients`, `Revenue` — на реда на **деня, в който заявката е дошла**, не деня на затваряне |
| **Седмично** | Преглед на изчислените колони |
| **Месечно** | `Qualified Leads` от CRM; преглед на CAC и ROAS |

### Защо приходът се записва на деня на заявката

Сделка, затворена през септември от заявка през август, принадлежи на
**августовския** разход. Записана на септемврийския ред, тя изкривява и двата
месеца — август изглежда губещ, септември изглежда безплатен.

Това е и причината `Lead ID` и `Created Date` да са задължителни в CRM
схемата: те са връзката, която прави правилното отнасяне възможно.

---

## Файл

`marketing/marketing-performance-template.csv` — само заглавен ред и една
празна редова заготовка. Без измислени стойности.

Формулите не се записват в CSV — те се добавят при импорт в Sheets или Excel
от секцията по-горе.

---

## Ограничения

| | |
|---|---|
| Реални данни | няма — акаунтите не съществуват още |
| Автоматично попълване | ръчно; автоматизацията е Phase 19 |
| Връзка с CRM | схемата е готова, инструментът липсва — Phase 20 |
| Часова ставка за истинска печалба | не е дефинирана |
